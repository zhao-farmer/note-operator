# 一、基础入门

## 1.1 认识文件目录


1. linux目录结构

    ![](/operations/system/linux/base/001.png)


2. 目录结构解释


      为了避免诸多使用者对Linux系统目录结构天马行空，Linux基金会发布了FHS标准。多数Linux发行版系统都遵循这一标准。  
      FHS（Filesystem Hierarchy Standard），文件系统层次化标准，该标准规定了 Linux 系统中所有一级目录以及部分二级目录（/usr 和 /var）的用途。发布此标准的主要目的就是为了让用户清楚地了解每个目录应该存放什么类型的文件。

    1. `/`

        根目录，位于Linux系统目录结构的顶层，一般根目录下只存放目录，不要存放文件。FHS认为，Linux系统的根目录最为重要，没有之一，原因有2点：

        *   所有目录都是由根目录衍生出来的
        *   根目录与系统的开机、修复、还原密切相关

    2. `/home` - 普通用户的家目录

        用户的主目录，在Linux中，每个用户都有一个自己的目录，一般该目录名是以用户的账号命名的，如上图中的alice、bob何eve。

    3. `/root` - 超级用户root家目录

        该目录为系统管理员，也称作超级权限者的用户主目录 。普通用户主目录在`/home`下，root主目录直接在`/`下。

    4. `/etc` - 系统配置文件的路径

        etc是Etcetera（等等）的缩写，配置文件保存目录。系统内所有采用默认安装方式（rpm 安装）的服务配置文件全部保存在此目录中，如用户信息、服务的启动脚本、常用服务的配置文件等。

        > **注意： 如果更改了该目录下的某个文件可能会导致系统不能启动 。**

    5. `/usr` - 存储系统软件资源

        usr 是 `unix shared resources`(共享资源) 的缩写，此目录用于存储系统软件资源。FHS建议所有的开发者，应把软件产品的数据合理的放置在`/usr`目录下的各子目录中，而不是为他们创建单独的目录。  
        Linux 系统中，所有系统默认的软件都存储在 `/usr` 目录下，`/usr` 目录类似 Windows 系统中 `C:\\Windows\\ + C:\\Program files\\` 两个目录的综合体。这是一个非常重要的目录，用户的很多应用程序和文件都放在这个目录下。

        *   `/usr/bin` 存放系统命令，普通用户和超级用户都可以执行。这些命令和系统启动无关，在单用户模式下不能执行
        *   `/usr/sbin` 存放根文件系统不必要的系统管理命令，如多数服务程序，只有root可以使用
        *   `/usr/lib` 应用程序调用的函数库保存位置
        *   `/usr/XIIP6` 图形界面系统保存位置
        *   `/usr/local` 手工安装的软件保存位置。一般建议**源码包软件**安装在这个位置
        *   `/usr/src` 源码包保存位置。我们**手工下载的源码包**和**内核源码包**都可以保存到这里。习惯把手工下载的源码包保存到 `/usr/local/src/` 目录中，把内核源码保存到 `/usr/src/linux/` 目录中
        *   `/usr/share` 应用程序的资源文件保存位置，如帮助文档、说明文档和字体目录
        *   `/usr/include` C/C++ 等编程语言头文件的放置目录

    6. `/var` - 存储动态数据

        var 是 variable(变量) 的缩写，/var 目录用于存储动态数据，例如缓存、日志文件、软件运行过程中产生的文件等。  
        
        系统上跑了很多程序，那么每个程序都会有相应的日志产生，而这些日志就被记录到这个目录下，具体在 `/var/log` 目录下，另外 mail 的预设放置也是在这里 。

        *   `/var/lib` 程序运行中需要调用或改变的数据保存位置。如 MySQL 的数据库保存在` /var/lib/mysql/` 目录中
        *   `/var/log `登陆文件放置的目录，其中所包含比较重要的文件如 /var/log/messages, /var/log/wtmp 等
        *   `/var/run` 一些服务和程序运行后，它们的 PID（进程 ID）保存位置
        *   `/var/spoo`l 里面主要都是一些临时存放，随时会被用户所调用的数据，例如 `/var/spool/mail/` 存放新收到的邮件，`/var/spool/cron/` 存放系统定时任务。
        *  ` /var/www` RPM 包安装的 Apache 的网页主目录
        *   `/var/nis`和`/var/yp `NIS 服务机制所使用的目录，nis 主要记录所有网络中每一个 client 的连接信息；yp 是 linux 的 nis 服务的日志文件存放的目录
        *   `/var/tmp` 一些应用程序在安装或执行时，需要在重启后使用的某些文件，此目录能将该类文件暂时存放起来，完成后再行删除

    7. `/opt` - 第三方安装的软件保存位置

        opt 是 optional(可选) 的缩写，这是给主机额外安装软件所摆放的目录。第三方安装的软件，手工安装的源码包软件都可以安装到这个目录中。默认是空的 。不过笔者还是习惯把软件放到 /usr/local/ 目录中，也就是说，/usr/local/ 目录也可以用来安装软件。

    8. `/bin` - 二进制文件 普通命令

        bin是Binaries（二进制文件）的缩写，存放系统命令，普通用户和root都可以执行。放在/bin下的命令在单用户模式下也可以执行。

    9. `/sbin` - `super bin`只有root用户才可以使用的命令库

        s 就是 Super User 的意思，是 Superuser Binaries (超级用户的二进制文件) 的缩写，这里存放的是系统管理员使用的系统管理程序 。保存与系统环境设置相关的命令，只有 root 可以使用这些命令进行系统环境设置，但也有些命令可以允许普通用户查看

    10. `/tmp` - 相当于Windows下的回收站

        tmp 是 temporary(临时) 的缩写， 临时目录。系统存放临时文件的目录，在该目录下，所有用户都可以访问和写入。建议此目录中不能保存重要数据，最好每次开机都把该目录清空。

        *   `/tmp`： 10天未用的文件，系统重启会清空目录
        *   `/var/tmp`：30天未用的文件，系统重启会清空目录
        *   `/usr/tmp`： 已经被众多发行版抛弃的临时文件夹

    11. `/dev` - Linux device设备（磁盘 光驱）

        dev 是 Device(设备) 的缩写, 该目录下存放的是 Linux 的外部设备，设备文件保存位置。在 Linux 中访问设备的方式和访问文件的方式是相同的。

    12. `/media` - 挂载目录

        挂载目录。系统建议用来挂载媒体设备，如软盘和光盘。linux 系统会自动识别一些设备，例如U盘、光驱等等，当识别后，Linux 会把识别的设备挂载到这个目录下。

    13. `/mnt` - 临时挂载点

        早期 Linux 中只有这一个挂载目录，并没有细分。系统建议这个目录用来挂载额外的设备，如 U 盘、移动硬盘和其他操作系统的分区。  
        
        系统提供该目录是为了让用户临时挂载别的文件系统的，我们可以将光驱挂载在 `/mnt/` 上，然后进入该目录就可以查看光驱里的内容了。

    14. `/lib` - 系统调用的函数库

        lib 是 Library(库) 的缩写，系统调用的函数库保存位置。这个目录里存放着系统最基本的动态连接共享库，其作用类似于 Windows 里的 DLL 文件。几乎所有的应用程序都需要用到这些共享库。

    15. `/boot` - 系统引导程序存放的目录

        系统启动目录，保存与系统启动相关的文件，如内核文件和启动引导程序（grub）文件等，不要轻易对该目录进行操作。

    16. `/selinux` - Windows下的防火墙

        这个目录是 Redhat/CentOS 所特有的目录，Selinux 是一个安全机制，类似于 windows 的防火墙，但是这套机制比较复杂，这个目录就是存放selinux相关的文件的。

    17. `/srv` - 服务数据目录

        服务数据目录。一些系统服务启动之后，可以在这个目录中保存所需要的数据。

    18. `/sys` - 虚拟目录
        
        虚拟文件系统。和 `/proc/` 目录相似，该目录中的数据都保存在内存中，主要保存与内核相关的信息。这是 Linux2.6 内核的一个很大的变化。该目录下安装了 2.6 内核中新出现的一个文件系统 sysfs 。

        *   sysfs 文件系统集成了下面3种文件系统的信息：针对进程信息的 proc 文件系统、针对设备的 devfs 文件系统以及针对伪终端的 devpts 文件系统。
        *   该文件系统是内核设备树的一个直观反映。
        *   当一个内核对象被创建的时候，对应的文件和目录也在内核对象子系统中被创建。

    19. `/proc` - 虚拟目录

        proc 是 Processes(进程) 的缩写，`/proc` 是一种伪文件系统（也即虚拟文件系统），存储的是当前内核运行状态的一系列特殊文件，这个目录是一个虚拟的目录，它是系统内存的映射，我们可以通过直接访问这个目录来获取系统信息。  

        虚拟文件系统。该目录中的数据并不保存在硬盘上，而是保存到内存中。主要保存系统的内核、进程、外部设备状态和网络状态等。如 `/proc/cpuinfo` 是保存 CPU 信息的，`/proc/devices` 是保存设备驱动的列表的，`/proc/filesystems` 是保存文件系统列表的，`/proc/net` 是保存网络协议信息的…

    20. `/run` - 虚拟目录

        是一个临时文件系统，存储系统启动以来的信息。当系统重启时，这个目录下的文件应该被删掉或清除。如果你的系统上有 `/var/run` 目录，应该让它指向 run。

    21. `/lost+found` - 系统异常关闭产生文件

        当系统意外崩溃或意外关机时，产生的一些文件碎片会存放在这里。在系统启动的过程中，fsck 工具会检查这里，并修复已经损坏的文件系统。这个目录只在每个分区中出现，例如，`/lost+found` 就是根分区的备份恢复目录，`/boot/lost+found` 就是 `/boot` 分区的备份恢复目录…


3. 总结

    - Linux的目录中有且只有一个根目录。
    - Linux的各个目录存放的内容是规划好，不用乱放文件。
    - Linux是以文件的形式管理我们的设备，因此linux系统，一切皆为文件。
    - Linux的各个文件目录下存放什么内容，必须有一个认识。

## 1.2 linux操作目录

### 1.2.1 `pwd` - 打印工作目录

- **作用**：显示当前工作目录的完整路径

- **命令参数**：

    ```bash
    pwd [选项]
    ```

- **常用选项**：

    *   `-L`（默认）：显示逻辑路径（包含符号链接）
    *   `-P`：显示物理路径（解析所有符号链接）
        

- **示例**：

    ```bash
    $ pwd
    /home/username
    $ pwd -P
    /usr/home/username  # 如果/home是符号链接
    ```

### 1.2.2 `cd` - 切换目录

- **作用**：更改当前工作目录

- **语法**：

    ```bash
    cd [目录路径]
    ```

- **特殊符号**：

    | 符号  | 含义  | 示例  |
    | --- | --- | --- |
    | `.` | 当前目录 | `cd .` |
    | `..` | 父目录 | `cd ..` |
    | `~` | 用户主目录 | `cd ~` |
    | `~username` | 指定用户的主目录 | `cd ~root` |
    | `-` | 前一个工作目录 | `cd -` |
    | `/` | 根目录 | `cd /` |

- **示例**：

    ```bash
    cd /usr/bin           # 切换到绝对路径
    cd Documents          # 切换到相对路径
    cd ../..              # 向上移动两级
    cd ~/Downloads        # 切换到主目录下的Downloads
    cd -                  # 返回上一个目录
    ```

### 1.2.3 `mkdir` - 创建目录

- **作用**：创建新的目录

- **命令参数**：

    ```bash
    mkdir [选项] 目录名...
    ```

- **常用选项**：

    | 选项  | 作用  | 示例  |
    | --- | --- | --- |
    | `-p` | 创建多级目录（父目录不存在时自动创建） | `mkdir -p a/b/c` |
    | `-m` | 设置目录权限模式 | `mkdir -m 755 dir` |
    | `-v` | 显示创建过程信息 | `mkdir -v newdir` |
    | `-Z` | 设置安全上下文（SELinux） | `mkdir -Z context dir` |

- **权限模式**：

    *   `755`：所有者rwx，组r-x，其他r-x
        
    *   `700`：所有者rwx，组---，其他---
        
    *   `777`：所有者rwx，组rwx，其他rwx
        

- **示例**：

    ```bash
    mkdir project                    # 创建单个目录
    mkdir dir1 dir2 dir3             # 创建多个目录
    mkdir -p data/{2024/{01,02},2023}  # 创建复杂目录结构
    mkdir -m 750 shared              # 创建具有特定权限的目录
    mkdir -vp logs/{access,error}    # 显示详细信息并创建多级目录
    ```

### 1.2.4 `rmdir` - 删除空目录

- **作用**：删除空的目录

- **命令参数**：

    ```bash
    rmdir [选项] 目录名...
    ```

- **常用选项**：

    | 选项  | 作用  | 示例  |
    | --- | --- | --- |
    | `-p` | 删除目录及其空的父目录 | `rmdir -p a/b/c` |
    | `-v` | 显示删除过程信息 | `rmdir -v emptydir` |
    | `--ignore-fail-on-non-empty` | 忽略非空目录错误 | `rmdir --ignore-fail-on-non-empty dir` |

- **示例**：

    ```bash
    rmdir emptydir          # 删除空目录
    rmdir dir1 dir2         # 删除多个空目录
    rmdir -p a/b/c          # 删除c目录，如果b变空则删除b，以此类推
    rmdir -v olddir         # 显示删除信息
    ```

### 1.2.5 `ls` - 列出目录内容

- **作用**：显示目录中的文件和子目录

- **命令参数**：

    ```bash
    ls [选项] [文件/目录...]
    ```

- **常用选项**：

    1. **显示格式选项**：

        | 选项  | 作用  | 示例  |
        | --- | --- | --- |
        | `-l` | 长格式显示（详细信息） | `ls -l` |
        | `-a` | 显示所有文件（包括隐藏文件） | `ls -a` |
        | `-A` | 显示除`.`和`..`外的所有文件 | `ls -A` |
        | `-F` | 添加类型标识符 | `ls -F` |
        | `-1` | 每行显示一个文件 | `ls -1` |
        | `-m` | 逗号分隔显示 | `ls -m` |
        | `-x` | 按行排列（默认） | `ls -x` |
        | `-C` | 按列排列 | `ls -C` |

    2. **排序选项**：

        | 选项  | 作用  | 示例  |
        | --- | --- | --- |
        | `-t` | 按修改时间排序 | `ls -t` |
        | `-S` | 按文件大小排序 | `ls -S` |
        | `-r` | 反向排序 | `ls -r` |
        | `-u` | 按访问时间排序 | `ls -u` |
        | `-c` | 按状态变更时间排序 | `ls -c` |

    3. **显示信息选项**：

        | 选项  | 作用  | 示例  |
        | --- | --- | --- |
        | `-h` | 人性化显示文件大小 | `ls -lh` |
        | `-i` | 显示inode编号 | `ls -i` |
        | `-n` | 显示UID和GID而非名称 | `ls -n` |
        | `-s` | 显示文件块大小 | `ls -s` |
        | `-R` | 递归显示子目录 | `ls -R` |
        | `-d` | 仅显示目录本身，不显示内容 | `ls -ld dirname` |
        | `-Z` | 显示SELinux安全上下文 | `ls -Z` |

    4. **常用组合**：

        ```bash
        ls -la           # 显示所有文件详细信息
        ls -lht          # 按时间排序，人性化显示大小
        ls -ltr          # 按时间反向排序（最旧在前）
        ls -lS           # 按大小排序
        ls -d */         # 仅列出目录
        ls -lh *.txt     # 显示所有txt文件详细信息
        ls -l --color=auto  # 彩色显示（默认）
        ls -l --time-style=long-iso  # ISO时间格式
        ```

    - **ls -l 输出详解**：

        ```text
        drwxr-xr-x  2 user group  4096 Jan 10 10:30 Documents
        ↑↑↑↑↑↑↑↑↑   ↑   ↑     ↑    ↑       ↑           ↑
        │││││││││   │   │     │    │       │           └─ 文件名
        │││││││││   │   │     │    │       └─ 修改时间
        │││││││││   │   │     │    └─ 文件大小（字节）
        │││││││││   │   │     └─ 所属组
        │││││││││   │   └─ 所有者
        │││││││││   └─ 硬链接数
        │└┴┴┴┴┴┴└─ 权限（9位）
        └─ 文件类型：d=目录，-=文件，l=链接，c=字符设备，b=块设备
        ```

### 1.2.6 `find` - 查找目录

- **作用**：在文件系统中查找目录

- **命令参数**：

```bash
find [路径] [表达式]
```

- **目录查找常用表达式**：

```bash
find / -type d              # 查找所有目录
find . -type d -name "log*" # 按名称查找目录
find /home -type d -empty   # 查找空目录
find . -type d -maxdepth 2  # 限制查找深度
find . -type d -mtime -7    # 查找7天内修改的目录
find . -type d -user root   # 查找root所有的目录
find . -type d -perm 755    # 查找权限为755的目录
```

### 1.2.7 `du` - 查看目录大小

- **作用**：估算目录磁盘使用情况

- **命令参数**：

    ```bash
    du [选项] [目录...]
    ```

- **常用选项**：

    | 选项  | 作用  | 示例  |
    | --- | --- | --- |
    | `-h` | 人性化显示大小（K，M，G） | `du -h dir` |
    | `-s` | 显示总计大小 | `du -sh dir` |
    | `-c` | 显示总计大小 | `du -ch dir1 dir2` |
    | `-a` | 显示所有文件和目录大小 | `du -ah dir` |
    | `-d` | 限制显示深度 | `du -d 1 dir` |
    | `--max-depth=N` | 同-d | `du --max-depth=2 dir` |
    | `--exclude=PATTERN` | 排除匹配的文件 | `du --exclude="*.log" dir` |
    | `--time` | 显示修改时间 | `du --time dir` |

- **示例**：

    ```bash
    du -sh /home               # 查看/home总大小
    du -h --max-depth=1        # 查看一级子目录大小
    du -ch *.log               # 查看多个文件大小并显示总计
    du -ah | sort -rh          # 显示所有文件大小并排序
    ```

### 1.2.8 其他相关命令

1. `tree` - 树状显示目录结构

    ```bash
    tree [选项] [目录]
    ```

    **常用选项**：

    *   `-L`：限制显示深度 `tree -L 2`
        
    *   `-d`：仅显示目录 `tree -d`
        
    *   `-a`：显示所有文件 `tree -a`
        
    *   `-I`：排除模式 `tree -I "*.log"`
        
    *   `-C`：彩色显示 `tree -C`
        
    *   `-h`：显示文件大小 `tree -h`
        

2. `dirs` - 显示目录栈

    ```bash
    dirs [选项]
    ```

    **常用选项**：

    *   `-v`：每行显示一个目录并编号
        
    *   `-p`：每行显示一个目录
        
    *   `-l`：展开`~`显示完整路径
        

3. `pushd`/`popd` - 目录栈操作

    ```bash
    pushd [目录]    # 进入目录并保存到栈
    popd           # 返回栈顶目录并移除
    ```


## 1.3 网络配置

## 1.4 远程登陆

## 1.5 系统管理



### 1.2.3 nano 编辑器

**nano**​ 是一个简单、直观的命令行文本编辑器。它的最大优点是 **“所见即所得”**​ ，你键盘上输入的字符会直接显示为文本，底部有清晰的快捷键提示，对新手非常友好。

1. 启动与退出

    1.  启动 nano

        ```bash
        # 编辑一个已存在的文件
        nano filename.txt

        # 创建一个新文件
        nano newfile.txt

        # 以超级管理员身份编辑系统文件（常用！）
        sudo nano /etc/hosts
        ```

    2. 界面介绍

        启动后，你会看到一个简单的界面：

        *  **顶部**：显示 nano 版本和当前编辑的文件名。如果是新文件，会显示 `New Buffer`。
            
        *  **中部**：文本编辑区域。
            
        *  **底部**：两行最重要的**快捷键提示栏**。
            

    3. 退出 nano

        这是最需要记住的第一个操作！

        *  按 `Ctrl + X`（底部显示为 `^X`）
            
        *   如果文件已修改，nano 会问你是否保存：
            
            *  按 `Y`：保存（Yes）
                
            *  按 `N`：不保存（No）
                
            *  按 `Ctrl + C`：取消退出操作
                
        *   如果保存的是一个新文件，或者你按了 `Y`，nano 会让你确认文件名，按 `Enter`键即可。
            
2. 核心快捷键详解


    **重要提示：**​ 在 nano 中，`^`符号表示 **Ctrl 键**; `M-` 符号代表 **Alt键**。例如 `^X`就是 `Ctrl + X`。

    1. 文件操作（最常用）

        | 快捷键 | 功能  | 说明  |
        | --- | --- | --- |
        | `Ctrl + S` | **保存文件**​ | 编辑中途随时保存，好习惯！ |
        | `Ctrl + O` | **另存为**​ | 输入新文件名后按回车 |
        | `Ctrl + X` | **退出**​ | 如果未保存，会提示保存 |

    2. 光标移动

        | 快捷键 | 功能  |
        | --- | --- |
        | `Ctrl + F` | 光标**向前**移动一个字符 |
        | `Ctrl + B` | 光标**向后**移动一个字符 |
        | `Ctrl + P` | 光标移动到**上一行**​ |
        | `Ctrl + N` | 光标移动到**下一行**​ |
        | `Ctrl + A` | 光标移动到**行首**​ |
        | `Ctrl + E` | 光标移动到**行尾**​ |
        | `Ctrl + V` | 向下翻页 |
        | `Ctrl + Y` | 向上翻页 |

    3. 编辑操作

        | 快捷键 | 功能  | 说明  |
        | --- | --- | --- |
        | `Ctrl + K` | **剪切**当前行 | 非常常用！ |
        | `Ctrl + U` | **粘贴**被剪切的内容 | 非常常用！ |
        | `Ctrl + ]` | 自动**补全**单词 | 类似 IDE 的代码补全 |
        | `Alt + U` | **撤销**上一步操作 |
        | `Alt + E` | **重做**被撤销的操作 |

    4. 搜索与替换

        | 快捷键 | 功能  | 说明  |
        | --- | --- | --- |
        | `Ctrl + W` | **搜索**​ | 输入要查找的文本 |
        | `Alt + W` | 搜索**下一个**匹配项 |     |
        | `Ctrl + \` | **搜索并替换**​ | 先输入搜索词，再输入替换词 |
        | `Alt + R` | 使用**正则表达式**搜索 | 高级功能 |

    5. 其他实用功能

        | 快捷键 | 功能  | 说明  |
        | --- | --- | --- |
        | `Ctrl + C` | 显示**光标位置**​ | 显示当前行号、列号 |
        | `Ctrl + G` | 显示**帮助文档**​ | 所有快捷键的完整列表 |
        | `Ctrl + T` | 运行**拼写检查**​ | 需要安装拼写检查器 |
        | `Alt + A` | 开启/关闭**标记**​ | 用于选择文本块 |

3. 实战练习


    - 步骤 1：创建并编辑文件

        ```bash
        nano my_script.sh
        ```

    - 步骤 2：输入内容

        在 nano 中输入以下内容：

        ```bash
        #!/bin/bash
        # This is my first script using nano
        echo "Hello, World!"
        echo "Current date is: $(date)"
        echo "Current user is: $USER"
        ```

    - 步骤 3：练习编辑操作

        1. **移动光标**：使用 `Ctrl + A`和 `Ctrl + E`跳到行首和行尾。
            
        2. **剪切一行**：把光标移到第三行，按 `Ctrl + K`剪切该行。
            
        3. **粘贴**：把光标移到最后，按 `Ctrl + U`粘贴。
            
        4. **搜索**：按 `Ctrl + W`，输入 `echo`搜索所有 echo 语句。
            

    - 步骤 4：保存并退出

        1.  按 `Ctrl + O`保存文件。
            
        2.  按 `Enter`确认文件名。
            
        3.  按 `Ctrl + X`退出 nano。
            
    - 步骤 5：测试脚本

        ```bash
        # 给脚本执行权限
        chmod +x my_script.sh

        # 运行脚本
        ./my_script.sh
        ```
    - 运行结果

        ![](/operations/system/linux/base/002.png)

4. 高级技巧

    1. 显示行号

        启动 nano 时使用 `-c`参数可以显示行号，对于调试代码和配置文件非常有用。

        ```bash
        nano -c myfile.txt
        ```

        或者在 nano 内部按 `Alt + C`来切换行号显示。

    2. 自动缩进

        编辑代码时，开启自动缩进非常方便。

        *  按 `Alt + M`可以开启/关闭自动缩进。
            
        *   或者启动时加 `-i`参数。
            

    3. 快速跳转到行号

        如果知道错误发生在某一行，可以快速跳转。

        *   按 `Ctrl + _`（下划线），然后输入行号，按回车。
            

    4. 编辑系统文件

        永远记得用 `sudo`来编辑系统文件，否则会没有权限保存。

        ```bash
        sudo nano /etc/hosts
        sudo nano /etc/nginx/nginx.conf
        ```
    5. 常试用配置参数

        ```sh
        -l # 行号
        -m #可以使用鼠标
        +n #跳转到某行
        ```

        ```sh
        # 调试时直接跳到出错行
        nano +45 my_script.py -l
        ```





### 1.2.1 echo 输出文本

`echo`命令用于在终端输出一行文本或变量的值。它就像是一个“回声”，你输入什么，它就输出什么。

**基本语法：**

```bash
echo [选项] [字符串/变量]
```

**常用选项：**

*  `-e`：启用反斜杠转义解释（用于处理换行符、制表符等）
    
*  `-n`：不输出末尾的换行符（内容输出后不换行）
    

**核心用法示例：**


1. 输出简单文本

    ```bash
    echo "Hello, World"
    # 输出：Hello, World

    echo Linux is powerful
    # 输出：Linux is powerful
    ```

2. 输出变量值

    ```bash
    # 设置变量
    name="Alice"
    version=$(uname -r)  # 获取内核版本

    # 输出变量
    echo "User: $name"
    # 输出：User: Alice

    echo "Kernel version: $version"
    # 输出：Kernel version: 5.15.0-91-generic
    ```

3. 使用转义字符（需要 `-e`选项）

    ```bash
    echo -e "Line 1\nLine 2\nLine 3"
    # 输出：
    # Line 1
    # Line 2
    # Line 3

    echo -e "Name:\tAlice\tAge:\t25"
    # 输出：Name:   Alice   Age:    25
    ```

4. 重定向输出到文件（创建/覆盖文件）

    ```bash
    # 创建新文件或覆盖已有文件内容
    echo "This is the first line" > example.txt

    # 追加内容到文件末尾
    echo "This is the second line" >> example.txt
    ```


### 1.2.2 `cat`命令：查看和连接文件

`cat`（concatenate的缩写）主要用于查看文件内容、创建小文件、合并文件。

**基本语法：**

```bash
cat [选项] [文件...]
```

**常用选项：**

*  `-n`：显示行号
    
*  `-b`：对非空行显示行号
    
*  `-A`：显示所有字符（包括换行符、制表符等）
    

**核心用法示例：**

1. 查看文件内容

    ```bash
    # 查看单个文件
    cat example.txt

    # 查看多个文件
    cat file1.txt file2.txt

    # 显示行号
    cat -n example.txt
    # 输出：
    # 1  This is the first line
    # 2  This is the second line
    ```

2. 创建小型文件

    ```bash
    # 从键盘输入创建文件（按 Ctrl+D 结束输入）
    cat > newfile.txt
    这是第一行
    这是第二行
    [按 Ctrl+D 保存]

    # 结合 heredoc 创建多行文件
    cat > script.sh << EOF
    #!/bin/bash
    echo "Hello from script"
    date
    EOF
    ```

3. 合并文件

    ```bash
    # 将 file1.txt 和 file2.txt 合并为 combined.txt
    cat file1.txt file2.txt > combined.txt

    # 追加文件内容
    cat file3.txt >> combined.txt
    ```

4. 查看特殊字符

    ```bash
    cat -A example.txt
    # 可以显示文件中的制表符（^I）、行结束符（$）等
    ```





## 1.2.6 `ln` - 创建链接（目录相关）

- **作用**：创建目录链接（主要是软链接）

- **命令参数**：

    ```bash
    ln [选项] 目标 链接名
    ```

- **常用选项**：

| 选项  | 作用  | 示例  |
| --- | --- | --- |
| `-s` | 创建软链接（符号链接） | `ln -s /usr/bin binlink` |
| `-f` | 强制创建，覆盖已存在的链接 | `ln -sf newtarget link` |
| `-i` | 覆盖前询问 | `ln -si target link` |
| `-n` | 如果目标是目录链接，作为普通文件处理 | `ln -sn dir link` |
| `-v` | 显示详细信息 | `ln -sv target link` |

- **示例**：

```bash
# 创建目录软链接
ln -s /var/log logs
ln -s /usr/local/bin mybin

# 更新现有链接
ln -sf /new/path existing_link

# 创建相对路径链接
ln -s ../data data_link
```
